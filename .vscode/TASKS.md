# 🚀 Быстрый запуск задач

## 🐳 Docker
- **Запуск стека**: `Ctrl+Shift+P` → "Tasks: Run Task" → "🐳 Docker: Start Dev Stack"
- **Остановка**: `Ctrl+Shift+P` → "Tasks: Run Task" → "🐳 Docker: Stop Dev Stack"
- **Логи бэкенда**: `Ctrl+Shift+P` → "Tasks: Run Task" → "🐳 Docker: View Logs (Backend)"

## 🐍 Django
- **Миграции**: `Ctrl+Shift+P` → "Tasks: Run Task" → "🐍 Django: Run Migrations"
- **Тесты**: `Ctrl+Shift+P` → "Tasks: Run Task" → "🐍 Django: Run Tests"
- **Shell**: `Ctrl+Shift+P` → "Tasks: Run Task" → "🐍 Django: Shell Plus"

## 🟢 Vue
- **Установка зависимостей**: `Ctrl+Shift+P` → "Tasks: Run Task" → "🟢 Vue: Install Dependencies"
